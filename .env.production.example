# =============================================================================
# 生产环境配置示例
# =============================================================================
# 使用方法：
#   1. 复制此文件：cp .env.production.example .env.production
#   2. 填写真实的密钥和配置
#   3. 部署时使用：docker-compose -f docker-compose.production.yml up -d
# =============================================================================

# =============================================================================
# 数据库配置
# =============================================================================
DB_NAME=law_education
DB_USER=law_edu_user
DB_PASSWORD=请设置一个强密码

# =============================================================================
# AI API 密钥
# =============================================================================
# DeepSeek API Key（必需）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# 302.ai API Key（用于PPT生成，必需）
NEXT_PUBLIC_AI_302_API_KEY=sk-your-302ai-api-key

# =============================================================================
# 前端配置
# =============================================================================
# 域名配置（根据实际情况修改）
NEXT_PUBLIC_BASE_URL=https://your-domain.com
NEXT_PUBLIC_SOCKET_IO_URL=https://your-domain.com

# =============================================================================
# 应用配置
# =============================================================================
# 游客模式（生产环境建议设为false）
GUEST_MODE=false

# 自动种子数据（首次部署设为true，后续设为false）
AUTO_SEED_DATABASE=true

# =============================================================================
# Docker镜像
# =============================================================================
# 阿里云镜像地址（从GitHub Actions推送的最新版本）
DOCKER_IMAGE=registry.cn-shenzhen.aliyuncs.com/yejunhao/legal-education:latest

# =============================================================================
# pgAdmin 配置（可选，仅用于数据库管理）
# =============================================================================
PGADMIN_EMAIL=admin@law-edu.local
PGADMIN_PASSWORD=请设置pgAdmin密码
