# ===================================
# AI配置 - 核心设置
# ===================================

# DeepSeek API配置（必需）
DEEPSEEK_API_KEY=你的API密钥
DEEPSEEK_API_URL=https://api.deepseek.com/v1

# 公开的API Key（用于客户端）
NEXT_PUBLIC_DEEPSEEK_API_KEY=你的API密钥
NEXT_PUBLIC_DEEPSEEK_API_URL=https://api.deepseek.com/v1

# ===================================
# AI模型参数（可选，有默认值）
# ===================================

# 默认模型
AI_MODEL=deepseek-chat

# 默认温度（0-1，越低越精确，越高越创造性）
AI_TEMPERATURE=0.7

# 最大Token数
AI_MAX_TOKENS=2000

# 超时设置（毫秒）
AI_TIMEOUT=30000

# 最大重试次数
AI_MAX_RETRIES=3

# ===================================
# 不同任务类型的温度配置（可选）
# ===================================

# 分析类任务（争议分析、证据分析等）- 更精确
AI_ANALYSIS_TEMPERATURE=0.5

# 生成类任务（故事生成、报告生成等）- 更有创造性
AI_GENERATION_TEMPERATURE=0.8

# 推理类任务（请求权分析、法律推理等）- 更严谨
AI_REASONING_TEMPERATURE=0.3

# 对话类任务（苏格拉底对话等）- 平衡
AI_DIALOGUE_TEMPERATURE=0.7

# ===================================
# 开发环境设置
# ===================================

NODE_ENV=development

# ===================================
# 课堂二维码配置（可选，但推荐配置）
# ===================================

# 公网或局域网访问地址（用于生成可扫描的二维码）
# 如果不配置，localhost环境下手机将无法扫码加入课堂
#
# 开发环境示例（局域网IP）：
# NEXT_PUBLIC_BASE_URL=http://192.168.1.100:3000
#
# 生产环境示例（域名）：
# NEXT_PUBLIC_BASE_URL=https://your-domain.com
#
# 如何查看本机IP：
#   - Windows: 运行 ipconfig，查找 IPv4 地址
#   - Mac: 运行 ifconfig，查找 en0 的 inet 地址
#   - Linux: 运行 ip addr 或 ifconfig

# ===================================
# 其他可选API（备用）
# ===================================

OPENAI_API_KEY=demo-key

# ===================================
# 302.ai API配置（PPT生成功能）
# ===================================

# 302.ai API Key（用于生成教学PPT）
# 获取步骤：
#   1. 访问 https://302.ai/ 注册账号
#   2. 登录后进入"API管理"页面
#   3. 创建新的API Key（新用户有免费额度）
#   4. API Key格式: sk-302ai-xxxxxxxxxxxxx
#
# 注意：
#   - 不配置此项将无法使用PPT生成功能
#   - 使用 AI_302_API_KEY（无 NEXT_PUBLIC_ 前缀），因为新架构使用服务端代理
AI_302_API_KEY=your-302ai-api-key-here